# Backend AI — never expose these to the frontend.

OPENAI_API_KEY=
AI_PROVIDER=openai
AI_MODEL=gpt-4o-mini
CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Com Docker: deixe Backend URL vazio no Settings (usa /api via Nginx).
# Sem Docker: Backend URL = http://127.0.0.1:8001
